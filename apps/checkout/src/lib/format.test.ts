import {describe,expect,it} from 'vitest';import {clock,formatEtb,normalizePhone,secondsRemaining} from './format';
describe('checkout formatting',()=>{it('formats ETB to two decimals',()=>expect(formatEtb('2000')).toBe('2,000.00'));it('never returns a negative countdown',()=>expect(secondsRemaining(1000,2000)).toBe(0));it('rounds countdown upward',()=>expect(secondsRemaining(2501,1000)).toBe(2));it('formats a countdown clock',()=>expect(clock(582)).toBe('09:42'));it('normalizes Ethiopian international numbers',()=>expect(normalizePhone('+251 92 844 4697')).toBe('0928444697'))})
